---
import Card from '../molecules/Card.astro'

const { jobs } = Astro.props

console.log(jobs)
---

<Card title="Job">
  <div class="flex justify-center items-center">
    <div class="w-full md:w-8/12 lg:w-6/12">
      <ol class="relative border-l-2 pb-1">
        {
          jobs.main && jobs.main.map((job:any) => (
            <li class="mb-10 ml-12 bg-card dark:bg-dark-card">
              <span class="absolute flex items-center justify-center w-12 h-12 rounded-full -left-6 ring-8">
                <img class="rounded-full" src="/images/job/ecc.jpg" alt="Bonnie image" />
              </span>
              <div class="p-4 border rounded-lg shadow-sm">
                <div class="items-center justify-between mb-3 sm:flex">
                  <time class="mb-1 text-sm sm:order-last sm:mb-0">{job.start} / {job.end}</time>
                  <div class="flex flex-wrap justify-center items-center">
                    <a href={job.url} class="font-bold text-highlight hover:underline">{job.name}</a>{job.joiningText}
                  </div>
                </div>
                {
                  job.description &&
                  <div class="px-4 py-2 text-sm border rounded-lg">
                    {job.description}
                  </div>
                }
              </div>
            </li>
          ))
        }
      </ol>
    </div>
  </div>
</Card>